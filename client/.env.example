# Copy this file to `.env` and adjust as needed.
# When unset, the app loads content from /api/*.json under the public folder.
# Point this to your CMS or backend to take content live without code changes.
# VITE_API_BASE_URL=https://your-cms.example.com/api
